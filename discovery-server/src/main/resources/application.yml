server:
  port: 8082
spring:
  application:
    name: smart-choice-discovery-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/
    register-with-eureka: false
    fetch-registry: false
application:
  description: Discovery Server Application